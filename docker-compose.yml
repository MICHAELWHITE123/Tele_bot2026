version: '3.8'

services:
  warehouse-bot:
    build: .
    container_name: warehouse-bot
    ports:
      - "8000:8000"
    env_file:
      - .env
    environment:
      - PORT=8000
    restart: unless-stopped
