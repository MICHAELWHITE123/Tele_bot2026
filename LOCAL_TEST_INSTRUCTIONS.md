# Инструкции по локальному запуску тестового бота

## Шаг 1: Установка зависимостей

Убедитесь, что у вас установлен Python 3.13 (или 3.11+). Затем установите все необходимые пакеты:

```bash
pip install -r requirements.txt
```

**Если возникает ошибка "Процесс не может получить доступ к файлу"** (файл занят другим процессом):
1. Закройте все запущенные процессы Python/uvicorn
2. Или используйте установку с флагом `--user`:
   ```bash
   pip install --user -r requirements.txt
   ```

Или установите вручную:

```bash
pip install fastapi==0.115.0 aiogram==3.13.0 google-api-python-client==2.152.0 google-auth==2.35.0 python-dotenv==1.0.1 uvicorn==0.32.0
```

## Шаг 2: Подготовка файла .env

Создайте файл `.env` в корне проекта со следующим содержимым:

```env
TELEGRAM_BOT_TOKEN=ваш_токен_бота_от_BotFather
GOOGLE_SERVICE_ACCOUNT_JSON=service_account.json
GOOGLE_SPREADSHEET_ID=ваш_spreadsheet_id
RAILWAY_ENV=development
```

### Как получить значения:

1. **TELEGRAM_BOT_TOKEN**: 
   - Откройте Telegram и найдите [@BotFather](https://t.me/BotFather)
   - Отправьте команду `/newbot` и следуйте инструкциям
   - Скопируйте полученный токен

2. **GOOGLE_SERVICE_ACCOUNT_JSON**: 
   - Путь к файлу `service_account.json` (должен быть в корне проекта)
   - Или укажите относительный путь, например: `service_account.json`

3. **GOOGLE_SPREADSHEET_ID**: 
   - Откройте вашу Google таблицу
   - ID находится в URL: `https://docs.google.com/spreadsheets/d/SPREADSHEET_ID/edit`
   - Скопируйте `SPREADSHEET_ID` из URL

## Шаг 3: Проверка файла service_account.json

Убедитесь, что файл `service_account.json` находится в корне проекта и содержит валидные учетные данные Google Service Account.

**Важно**: Service Account должен иметь доступ к вашей Google таблице (поделитесь таблицей с email из service_account.json).

## Шаг 4: Запуск тестового скрипта

Запустите локальный тестовый скрипт:

```bash
python test_bot_local.py
```

## Что делает скрипт:

1. ✅ Проверяет наличие всех переменных окружения из `.env`
2. ✅ Проверяет подключение к Google Sheets
3. ✅ Выводит первые 5 строк листа ITEMS
4. ✅ Запускает бота в режиме polling (без webhook)
5. ✅ Ожидает сообщения в Telegram

## Использование бота:

1. Найдите вашего бота в Telegram (по имени, которое вы указали при создании)
2. Отправьте команду `/start`
3. Отправьте любой `inventory_id` из столбца K вашей таблицы как текстовое сообщение
4. Бот найдет товар и поставит `TRUE` в столбце T

## Пример работы:

```
Пользователь отправляет: ABC123
Бот отвечает: 🔍 Поиск: ABC123...
Бот отвечает: ✅ Наклейка отмечена для inventory_id: ABC123 (строка 5)
```

В консоли вы увидите:
```
📨 Получено сообщение от 123456789: ABC123
📤 Результат: ✅ Наклейка отмечена для inventory_id: ABC123 (строка 5)
```

## Остановка бота:

Нажмите `Ctrl+C` в терминале для остановки бота.

## Устранение проблем:

### Ошибка: "TELEGRAM_BOT_TOKEN not set"
- Проверьте, что файл `.env` существует в корне проекта
- Проверьте, что в `.env` есть строка `TELEGRAM_BOT_TOKEN=...`
- Убедитесь, что используется `python-dotenv` и `load_dotenv()` вызывается

### Ошибка: "GOOGLE_SERVICE_ACCOUNT_JSON env variable not set"
- Проверьте путь к `service_account.json` в `.env`
- Убедитесь, что файл существует

### Ошибка подключения к Google Sheets
- Проверьте, что `service_account.json` валиден
- Убедитесь, что Service Account имеет доступ к таблице
- Проверьте правильность `GOOGLE_SPREADSHEET_ID`

### Бот не отвечает
- Проверьте, что токен бота правильный
- Убедитесь, что бот запущен (нет ошибок в консоли)
- Проверьте интернет-соединение
