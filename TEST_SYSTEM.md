# 🧪 Инструкция по проверке всей системы

## Быстрая проверка

Запустите комплексный тест:

```bash
python test_full_system.py
```

Этот скрипт проверит:
- ✅ Переменные окружения (.env)
- ✅ Подключение к Google Sheets
- ✅ Все методы GoogleSheetsClient
- ✅ Все FastAPI эндпоинты
- ✅ WebApp интерфейс

## Пошаговая проверка вручную

### 1. Проверка переменных окружения

Убедитесь, что файл `.env` содержит:
```env
TELEGRAM_BOT_TOKEN=ваш_токен
GOOGLE_SERVICE_ACCOUNT_JSON=service_account.json
GOOGLE_SPREADSHEET_ID=ваш_id
RAILWAY_ENV=development
```

### 2. Проверка Google Sheets

```bash
python -c "from app.google_sheets import GoogleSheetsClient; client = GoogleSheetsClient(); print(f'Строк: {len(client.get_all_items())}')"
```

### 3. Запуск FastAPI сервера

```bash
uvicorn app.main:app --reload
```

Сервер запустится на `http://localhost:8000`

### 4. Проверка эндпоинтов в браузере

- **Главная страница**: http://localhost:8000/ (редирект на /webapp)
- **WebApp**: http://localhost:8000/webapp
- **Swagger UI**: http://localhost:8000/docs
- **ReDoc**: http://localhost:8000/redoc
- **Health check**: http://localhost:8000/health

### 5. Тестирование API через Swagger

1. Откройте http://localhost:8000/docs
2. Протестируйте эндпоинты:
   - `GET /items` - получить все элементы
   - `GET /items/{inventory_id}` - получить конкретный элемент
   - `POST /items/check` - отметить элемент
   - `POST /items/uncheck` - снять отметку

### 6. Тестирование WebApp

1. Откройте http://localhost:8000/webapp
2. Введите `inventory_id` из вашей таблицы
3. Или нажмите кнопку 📷 для сканирования QR (в Telegram)
4. Проверьте отображение информации (столбцы B и V)
5. Нажмите кнопки "✅ Отметить" / "❌ Снять отметку"

### 7. Тестирование Telegram бота

```bash
python test_bot_local.py
```

Или используйте основной бот:
```bash
python -m app.bot
```

## Проверка через curl (опционально)

### Получить все элементы:
```bash
curl http://localhost:8000/items
```

### Получить конкретный элемент:
```bash
curl http://localhost:8000/items/ВАШ_INVENTORY_ID
```

### Отметить элемент:
```bash
curl -X POST http://localhost:8000/items/check \
  -H "Content-Type: application/json" \
  -d '{"inventory_id": "ВАШ_INVENTORY_ID"}'
```

### Снять отметку:
```bash
curl -X POST http://localhost:8000/items/uncheck \
  -H "Content-Type: application/json" \
  -d '{"inventory_id": "ВАШ_INVENTORY_ID"}'
```

## Ожидаемые результаты

### ✅ Успешная проверка:

1. **test_full_system.py** выводит все ✅
2. **FastAPI** запускается без ошибок
3. **WebApp** открывается и показывает интерфейс
4. **API эндпоинты** возвращают корректные данные
5. **Telegram бот** отвечает на сообщения

### ❌ Возможные проблемы:

- **Ошибка подключения к Google Sheets**: проверьте service_account.json и доступы
- **404 на эндпоинтах**: убедитесь, что сервер запущен
- **Пустые данные**: проверьте, что в таблице есть данные в листе ITEMS
- **Ошибка поиска**: убедитесь, что inventory_id существует в столбце K

## Полная интеграция

Для полной проверки всех компонентов вместе:

1. **Терминал 1**: Запустите FastAPI
   ```bash
   uvicorn app.main:app --reload
   ```

2. **Терминал 2**: Запустите бота
   ```bash
   python test_bot_local.py
   ```

3. **Браузер**: Откройте http://localhost:8000/webapp

4. **Telegram**: Откройте бота и нажмите кнопку "📷 Open QR Scanner"

Все компоненты должны работать вместе! 🎉
